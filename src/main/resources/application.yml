spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/blog
    username: user
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
